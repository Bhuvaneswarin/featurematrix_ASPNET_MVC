@using Syncfusion.EJ2
@model EJ2MvcEditors.Controllers.DateTimePicker.DateTimePickerValue
<h3> DateTimePickerFor Sample: </h3>
<h5>DateTimePickerFor sample</h5>
<br />
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    <div class="col-lg-12 control-section">
        <div id="wrapper">
            @Html.EJS().DateTimePickerFor(model => model.value).Format("dd-MMM-yy hh:mm a").Placeholder("Choose a date and time").Width("250px").Render()
            <div id="errorMessage">
                @Html.ValidationMessageFor(model => model.value)
            </div>
            <div id="submitbutton">
                @Html.EJS().Button("btn").Content("Post").Render()
            </div>
        </div>
    </div>
}
<style>
    #errorMessage {
        color: red;
        text-align: center
    }

    #wrapper {
        max-width: 246px;
        margin: 0px auto;
    }

    #submitbutton {
        margin-top: 20px;
        margin-left: 65px;
    }

    #control-content #wrapper .field-validation-error {
        color: red;
    }
</style>
